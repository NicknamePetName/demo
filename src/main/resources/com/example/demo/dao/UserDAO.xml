<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.UserDAO">

    <resultMap id="userResultMap" type="com.example.demo.dataobject.UserDO">
        <id column="id" property="id"/>
        <result column="user_name" property="userName"/>
        <result column="password" property="password"/>
        <result column="nick_name" property="nickName"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="avatar" property="avatar"/>
        <result column="gmt_created" property="gmtCreated"/>
        <result column="gmt_modified" property="gmtModified"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.demo.dataobject.UserDO" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO user(user_name,password,nick_name,email,phone,avatar,gmt_created,gmt_modified)
        VALUES (#{userName},#{password},#{nickName},#{email},#{phone},#{avatar},now(),now())
    </insert>

    <select id="findByUserName" resultMap="userResultMap" parameterType="java.lang.String">
        SELECT id,user_name,password,nick_name,email,phone,avatar,gmt_created,gmt_modified
        FROM user
        WHERE user_name = #{userName}
    </select>

</mapper>